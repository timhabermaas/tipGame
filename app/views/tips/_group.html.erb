<li class="group">
  <ul>
    <h<%= level %>><%= group %></h<%= level %>>
      <% matches.each do |match| %>
        <%= render :partial => "tips/match", :locals => { :muh => match, :tip => @tips.where(:match_id => match.id).first } %>
      <% end %>
  </ul>
</li>