<li class=<%= match.running? ? "'running'" : "'inactive'"%>>
  <% if admin? %>
    <em class="id"><%= link_to match.id, edit_match_path(match) %></em>
  <% end %>
  <em class="team"><%= truncate(match.team_1_name, :length => 11, :omission => ".") %></em>
  <em class="score"><%= "#{match.score}" %></em>
  <em class="team"><%= truncate(match.team_2_name, :length => 11, :omission => ".") %></em>
  <small class="date"><%= match.starts_at %></small>
  <%= image_tag("football.png", :alt => "Fußball", :border => 0, :width => '20px') if match.running? %>
</li>